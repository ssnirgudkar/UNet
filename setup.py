from setuptools import setup

setup(
   name='unetpackage',
   version='1.0',
   description='Unet code package',
   author='shailesh',
   author_email='foomail@foo.com',
   packages=['unetpackage'],  #same as name
   install_requires=[ ], #external packages as dependencies
)
